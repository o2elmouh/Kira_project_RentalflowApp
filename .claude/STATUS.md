# RentaFlow — Active Sprint Status

> Update this file after every successful push. Do NOT edit CLAUDE.md for version or task changes.

---

## Pending Tasks
- Wire Resend email provider (`server/routes/email.js` — needs `RESEND_API_KEY`)
- **Law 09-08 compliance:** Phase 5 in progress — 5a (v1.10.6) + 5b (v1.10.7) shipped. Frontend now hits `/clients` API for all client CRUD. **5c (v1.10.8) deferred until pre-prod cutover** — no real PII exists yet, so the 2-week-soak flip-flag procedure is theatrical on staging. At prod cutover: set `ENCRYPT_PII=true` in Railway from day one + ship a single migration that drops the plaintext columns before launch. Skip the backfill (`npm run migrate:clients-encrypt`) and the soak. Optional dress rehearsal: run the backfill once against staging Supabase to confirm it executes cleanly. See [.claude/PHASE_PLAN.md](PHASE_PLAN.md).
- **Railway cron:** schedule `cleanup:pending` daily at 03:00 UTC in Railway dashboard (Phase 2)

---

## Versioning
- Version is displayed in the Sidebar bottom-left, next to the agency name.
- **Every push must bump the version** in `components/Sidebar.jsx` (the `v1.x.x` string).
- **Increment rule:** patch (+0.0.1) for fixes/small changes, minor (+0.1.0) for new features.
- **Every staging push must add a row to the Staging Deployments table below.**

---

## Staging Deployments
| Version | Commit | What's in it |
|---|---|---|
| v1.16.1 | pending | **Fix v1.16.0 — accounting dashboards were showing 0,00 MAD despite 17 closed contracts**. Three real bugs, all in one push. **Bug A (silent killer)**: `utils/accounting.js#generateRentalInvoice` read `contract.totalHT` / `tva` / `extraKmFee` / `fuelFee` / `damageFee` — **none of those columns exist on `contracts`** (the schema only has `total_amount` → `totalTTC` via contractFromDb, plus a lumped `extra_fees`). So it built `Debit 1100 = totalTTC` with zero offsetting credits, threw "Transaction déséquilibrée", and the non-blocking try/catch in `Step4Closure.handleClose` swallowed the error. Every restitution closure since v1.16.0 silently failed to post journal entries. Fix: derive `totalHT = totalTTC / 1.20` and `tva = totalTTC - totalHT` at the Moroccan 20% rate when the contract doesn't carry them explicitly; honor an explicit HT/TVA if ever present (forward-compat); route the lumped `extra_fees` column to a 3020 (Frais de restitution) credit line and subtract from `baseCA`; throw a clear "Contrat XXX sans montant — facture non émise" only when `totalTTC <= 0` (catches truly empty contracts instead of silently dropping). Same shape as the mobile port from the v1.16.0 mobile commit. **Bug B (dashboard zeros)**: `pages/accounting/PnLView.jsx` and `pages/accounting/UtilizationView.jsx` both read `Number(c.totalHT) || 0` in the closed-contracts revenue reducers. Same root cause: `totalHT` doesn't exist → every contract contributes 0. Fix: read `c.totalHT` first (forward-compat), fall back to `c.totalTTC ?? c.total_amount` derived at 1.20. This is why your staging screenshots showed "Revenus de location: 0,00 MAD — Basé sur 17 contrat(s) clôturé(s)" and "REVENU HT: 0,00 MAD" for BYD Atto 3 / Honda Jazz / Audi A1 — the closed-contract count was correct but the per-row reduction was zeroing out. **Bug C (orphan historical data)**: even after fixing A+B, the 17 closed contracts that pre-date the v1.16.0 wiring had no journal entries — Bilan agence stays empty until they're posted. Added `backfillJournalForClosedContracts()` in `utils/accounting.js` (idempotent: skips any contract whose id is already in `transactions.contract_id` for an `invoice` or `manual` type; returns `{ created, skipped, errors: [{ contractId, contractNumber, message }] }`) and wired a **"Régénérer écritures"** outline button on the Bilan agence tab (admin-only by virtue of the parent route guard) that calls it through a confirm prompt and surfaces the result inline (green for success, red for errors; console.warn drops the full error list when any). Plus 6 new tests in `src/test/accounting.test.js`: derives-from-totalTTC, reads-total_amount-snake_case, lumped-extra_fees-routes-to-3020, refuses-when-no-amount, backfill-skips-already-posted, backfill-captures-per-contract-errors-without-aborting. Suite **454/454** (was 448). The Step4Closure / ContractStep wiring from v1.16.0 is unchanged — the fix is entirely in the helper + the views; the existing journal-entry-on-close path now actually produces balanced entries. **What the user needs to do on staging**: navigate to Comptabilité → Bilan agence → click "Régénérer écritures" once. The 17 closed contracts get journal entries (totalTTC split across 1100/3000/3020/2100) and every dashboard tab populates. Bug A's fix means future closures post entries automatically. |
| v1.16.0 | pending | **Comptabilité feature — make it actually work end-to-end**. Audit from this session found the Accounting page was scaffolded (UI, tabs, helpers) but every write path was broken by schema/field-name mismatches and the feature had zero integration with the rental flow → empty dashboards in production. Five-step fix, all in one push: **(1) New migration `20260608_accounting_schema_fix.sql`** adds the columns the code writes but the original migrations never created (`transactions.total_amount` + `invoice_id`; `journal_entries.transaction_ref` + `account_name`; `deposits.client_name` + `vehicle_name` + `deductions` jsonb + `released_amount` + `transaction_id` + `release_transaction_id`); widens `deposits.status` check to include `partially_released` + `retained` (the values `releaseDeposit` emits); creates a `seed_default_accounts()` SECURITY DEFINER trigger on `agencies AFTER INSERT` that seeds the 17-account Moroccan chart of accounts (`1000`/`1010`/`1100`/`1200`/`1300`/`2000`/`2100`/`2200`/`3000`/`3010`/`3020`/`3030`/`4000`/`4010`/`4020`/`4030`/`4040`); backfills the chart for every existing agency via a `DO $$ ... LOOP` block; 6 helpful indexes. Previously `lib/db.js#initDefaultAccounts` was a no-op stub commenting "seeded via migration" but no migration ever did — new agencies started with zero accounts so every `postTransaction` resolved account names to undefined and TabBilan was empty. **(2) camelCase↔snake_case mappers in `lib/db.js`** (`accountToDb`/`FromDb`, `transactionToDb`/`FromDb`, `journalEntryToDb`/`FromDb`, `depositToDb`/`FromDb`) — the previous `sbUpsert` just spread the row verbatim so `saveTransaction({ totalAmount, contractId, invoiceId })` sent those literal camelCase column names → PostgREST 42703 reject on first call. `transactionToDb` also auto-generates a `TXN-YYYYMMDD-XXXX` reference when missing so the journal always has a human-readable transaction ID. **(3) Flow integration**: `pages/rental/ContractStep.jsx#handleFinalize` now calls `holdDeposit` after the vehicle flip to `rented` (skipped when `rental.deposit ?? rental.depositAmount` is 0; non-blocking on error so accounting failures never block the rental); `pages/restitution/Step4Closure.jsx#handleClose` now calls `generateRentalInvoice(contract.id)` after the contract update (steps 1-4) then looks up any held deposit via `getDepositByContract` and calls `releaseDeposit` with deductions mapped to revenue accounts (extraKm→3030, fuel→3020, damage→3020). Both calls wrapped in try/catch with `console.warn` so accounting failures never block the user flow. Previously zero file under `pages/Restitution.jsx` or `pages/rental/` called accounting helpers — the journal stayed empty regardless of business activity. **(4) TanStack Query hooks** (`src/hooks/useAccounting.js` with `useAccounts` 5min staleTime / `useTransactions` / `useJournalEntries` / `useDeposits` 60s) and migrated `TabDashboard` + `TabJournal` + `TabPlanComptable` + `TabDeposits` off `useEffect+useState+useCallback(load)` patterns. TabBilan kept on `useEffect+computeAgencyPayout` because each date range is its own snapshot — caching wouldn't help. **(5) Tests + schemaCheck extension**: new `src/test/accounting.test.js` ×16 (postTransaction balance check + balanced persist + rounding tolerance + unknown account fallback; generateRentalInvoice missing-contract throw + plain rental + extras split; holdDeposit balanced 1200/2000 + deposit row write; releaseDeposit full refund + partial + retained + unknown id; computeAgencyPayout classification + date filter + orphan-code ignore; computePL pass-through), new `src/test/hooks/useAccounting.test.jsx` ×4 (each hook's basic shape + cache hit on remount); `scripts/schemaCheck.js` now probes 53 columns across the 4 accounting tables. Full suite **448/448** (was 428). **Bonus bug surfaced by test #11**: `utils/accounting.js#releaseDeposit` was emitting unbalanced journal entries when deductions exceed the deposit (debits=3000 vs credits=3500 in the scenario) because the `refundAmount > 0` branch was the ONLY way to absorb the excess and the `refundAmount < 0` case had no counterpart. Added a `Debit 1100 (Créances clients)` line for `-refundAmount` so the journal stays balanced and the excess client debt lands on the correct receivable account. **Plus: Comptabilité now a primary sidebar item** — added between Réservations and Boîte de réception with the `BookOpen` icon, gated admin-only (added `'accounting'` to `ADMIN_ONLY_PAGES` alongside `'settings'`). `App.jsx case 'accounting'` renders `<Accounting />` directly with an `isAdmin` redirect-to-dashboard guard (matches Settings pattern). Removed the Accounting tab from `pages/Documents.jsx` (it was previously the admin-only third tab under Documents) — Documents now only shows Contrats + Factures. i18n key `nav.accounting` already wired in fr/ar/en ("Comptabilité"/"المحاسبة"/"Accounting") so no locale changes needed. The legacy `nav.documentsAccounting` key is now orphaned but left in place (removing it is unrelated cleanup with non-zero risk of breaking a stray reference). **Migration ordering nuance for cutover**: this migration assumes the `accounts`/`transactions`/`journal_entries`/`deposits` tables already exist (they're created in `001_initial_schema.sql` / `002_patch_existing_schema.sql` / `005_missing_tables.sql`). Production Supabase (`apzarvjxvwtlphdqirjm`) is documented in v1.15.2 as severely behind migrations — all four base tables plus everything past v1.5.0 will need to be applied before this migration can run there. |
| v1.15.2 | pending | **Audit-pass fixes**. Cross-checked features that were miss-planned or miss-developed in the v1.14.x → v1.15.x window and fixed each: (1) **`lib/db.js#findVehicleConflicts` was silently broken** — introduced in v1.14.20 with `.select('id, contract_number, pickup_date, return_date, client_name, vehicle_id')`. The `client_name` column doesn't exist on `contracts` (never has — denormalized fields were never added to the schema), so PostgREST was failing the whole query with 42703 and the LeadModal conflict-warning feature has been a no-op since v1.14.20. Removed `client_name` from the SELECT and from the returned shape (callers only read `.startDate`/`.endDate` — the vehicle label already comes from `targetContract`, not the conflict row). (2) **Calendar page never migrated** — plan-of-record gap; switched `pages/Calendar.jsx` from `useState + useEffect + Promise.all([getContracts(), getFleet()])` to `useFleet` + `useContracts` hooks. Same cache-hit benefit as Fleet/Contracts pages on tab re-navigation. (3) **Dashboard page same gap** — `pages/Dashboard.jsx` was fetching 4 datasets (`fleet`, `clients`, `contracts`, `invoices`) on every mount via `Promise.all`. Created new `src/hooks/useInvoices.js` to round out the set, then swapped Dashboard to `useFleet` + `useClients` + `useContracts` + `useInvoices`. (4) **New `scripts/schemaCheck.js` + `npm run schema:check`** — fail-fast probe that hits `/rest/v1/<table>?select=<col>&limit=1` for every column the code references. Catches the class of regression that bit v1.15.0 (explicit SELECT listing a non-existent column → 42703 → empty page). Vitest mocks the supabase client so this kind of schema-drift bug only surfaces against a real DB; the script closes that gap. Verified against staging: **153/153 columns OK** (also confirmed `pending_demands.accepted_at`, `docs_completed_at`, `gmail_message_id`, `prolongation_target_contract_id`, all `agencies.*`, all `clients.*_enc` columns exist on staging). Production Supabase (`apzarvjxvwtlphdqirjm`) is severely behind migrations — virtually everything past v1.5.0 will fail there until migrations are applied; capture as cutover blocker before any prod traffic. Full suite **428/428** passing. |
| v1.15.1 | pending | **HOTFIX: revert narrow SELECTs (Tasks 8+9 from v1.15.0 broke prod)**. Symptom: Fleet, Documents (Contracts), and Calendrier pages all rendered empty after v1.15.0 push. Root cause: Tasks 8 + 9 replaced `sbSelect('table')` (which uses `select('*')`) with explicit column lists for `getContracts`, `getFleet`, `getInvoices`. PostgREST fails the ENTIRE query with `42703 column X does not exist` when any listed column is missing on the live schema, whereas `select('*')` tolerantly returns whatever columns actually exist. Production schema is BEHIND several recent migrations and is missing: `vehicles.category` (frontend-only field defaulted by the mapper, never a real column), `contracts.client_name` / `vehicle_name` / `signature_status` / `signed_at` / `signed_pdf_path` / `finalized_at` / `prolongation_target_contract_id` (newer feature columns from recent migrations not yet applied to prod). Tests passed in v1.15.0 because vitest mocks the Supabase client — the missing-column failure only manifests against a live DB. Fix: reverted `getFleet`, `getContracts`, `getInvoices` back to `sbSelect()` (one-line bodies). Lose ~30% payload trimming in exchange for schema-drift tolerance; the byte savings weren't worth this regression risk. Long-term: pin the explicit-column approach to a per-environment column manifest, OR apply all pending migrations to prod first. Full suite **428/428** still passing (the existing tests don't exercise the schema-mismatch path — gap is documented; a future migration-applied check at startup would catch this earlier). Calendar page never used hooks, but it imports `getContracts` + `getFleet` directly — that's why it ALSO went empty even though it wasn't touched by the v1.15.0 plan. |
| v1.15.0 | pending | **Data-fetch optimization** (plan `docs/superpowers/plans/2026-06-05-data-fetch-optimization.md`, Tasks 1-10). Migrated Clients / Fleet / Contracts / Basket pages from `useEffect+useState+Promise.all` to TanStack Query hooks: `src/hooks/useAgency.js` (T1), `src/hooks/useClients.js` + `useFleet.js` + `useContracts.js` (T2), then page swaps for `pages/Clients.jsx` (T3, 3 `reload()` → `invalidateClients`), `pages/Fleet.jsx` (T4, 3 `refresh()` → `invalidateFleet`), `pages/Contracts.jsx` (T5, was 4 parallel fetches on every mount → cached) and `hooks/useLeads.js` rewrite (T6, return-shape preserved so `pages/Basket.jsx` didn't need changes). Tab navigation now hits the React Query cache (~30-60s `staleTime` per dataset) instead of refetching from Supabase. `components/Sidebar.jsx` polling reduced from 1s → 10s via new `src/hooks/useSidebarCounts.js` (T7, ~3600 → 360 req/hr/session, 10× load reduction) and nav-hover prefetch via `useQueryClient().prefetchQuery` on `onMouseEnter` (T10) warms the cache before the click — re-navigation to Clients / Fleet / Contracts / Basket is now <50ms cache hit instead of 700-2000ms cold load. Backend payload trimming: `lib/db.js#getContracts` (T8, 33 explicit cols vs. `select('*')`), `getFleet` (T9, 26 cols), `getInvoices` (T9, 14 cols) — drops encrypted PII columns and other unused fields off the wire. Tests: +13 new (`src/test/hooks/useAgency.test.jsx` ×2, `useClients.test.jsx` ×4, `useLeads.test.jsx` ×4, `useSidebarCounts.test.jsx` ×2 plus suite parity check). Full suite **428/428** passing (was 415 pre-plan). Settings page (`pages/OtherPages.jsx`) deferred — its delegated tab components fetch independently and can be migrated 1:1 using the same hooks. |
| v1.14.24 | pending | **Code-review fixes (audit pass)**. Findings + Low items captured in `CODE_REVIEW_FINDINGS.md` at repo root. Fixed inline: (1) **`server/routes/leads.js`** — `VALID_CLASSIFICATIONS` + `VALID_PATCH_CLASSIFICATIONS` were stale (`['lead','normal','spam']`), but the pipeline writes `'new_lead'`/`'prolongation'`/`'support_issue'`/`'alert'`. GET /leads with `?classification=new_lead` and PATCH `/leads/:id/status` with the same would 400. Added canonical values + kept legacy for backward compat. (2) **`server/lib/contractSigning.js`** — `prepareSignableContract` HMAC'd signing tokens with `'fallback-dev-key'` when no secret was configured; in production that key is checked into source so anyone could forge tokens. Now throws when `NODE_ENV=production` AND neither `SIGNING_TOKEN_SECRET` nor `ENCRYPTION_KEY` is set. Dev fallback preserved. (3) **`server/lib/contractSigning.js`** — sign-URL fallback `'https://app.rentaflow.local'` would have produced dead links if `FRONTEND_URL` were missing in prod; now throws under the same NODE_ENV guard. (4) **`server/routes/leads.js#getClientStatus`** — used `senderJid.replace('@s.whatsapp.net','').replace(/\D/g,'')` then matched against `clients.phone`. For `@lid` senders the digit prefix is NOT a phone, so the lookup either always missed OR (worse) false-positived against an unrelated client whose phone happened to share the digits. Added an explicit `@lid` short-circuit returning `'no_contract'`. (5) **`server/routes/leads.js#findActiveLeadByPhone`** — same root cause, different symptom: matched by last-9-digit suffix without checking JID shape, so `212658100534@s.whatsapp.net` and `7383658100534@lid` could collide. Gated by sender-shape (both `@lid` or both phone) before the digit match. Full suite **416/416** still passing — no test changes needed since the affected paths weren't covered before (gap captured in CODE_REVIEW_FINDINGS L8). |
| v1.14.23 | pending | **Two NewRental fixes**. (1) **Reservation total_price always 0** — `utils/reservationPayload.js#buildReservationPayload` read `rental?.totalPrice ?? rental?.total ?? 0`, but the wizard's `rental` state uses **`totalTTC`** as the canonical field (set in `pages/rental/RentalStep.jsx:67-78`, surfaced in `pages/rental/ContractStep.jsx:117`). Neither `totalPrice` nor `total` was ever set, so every reservation row landed with `total_price = 0`. Existing tests passed only because they stubbed `totalPrice: 1200` directly — a tests-the-mock trap. Fix: prepend `rental?.totalTTC` to the lookup chain (`totalPrice` / `total` kept as backward-compat fallbacks). +2 tests in `src/test/reservationPayload.test.js` (canonical totalTTC path, precedence-over-legacy-fields). (2) **Vehicle prematurely flagged `rented` when signing link sent** — `pages/rental/ContractStep.jsx#ensureContract` did `await saveVehicle({ ...v, status: 'rented' })` immediately after creating the contract row. ensureContract fires when the agent picks a signing channel (WhatsApp/Email), which dispatches the signing link BEFORE the client signs anything. Cars stayed locked as `rented` if the client never signed or the agent closed the modal without clicking "Annuler la location". Fix: moved the vehicle.status flip out of `ensureContract` and into `handleFinalize` (the "Finaliser le contrat" CTA — fires only when the agent confirms the rental is real). **Crucial nuance**: the date range stays protected against double-booking the whole time because `get_available_vehicles` RPC keys on `contract.status='active' AND date overlap`, not on `vehicle.status`. The flip is purely the physical-state hint shown in Fleet / Dashboard / FleetMap. `revertEnsured`'s defensive `if (v.status === 'rented') → 'available'` flip-back is preserved so any in-flight contract from prior code still gets cleaned up on Annuler. Full suite 416/416 passing. |
| v1.14.22 | pending | **Skip Baileys URL preview generation on outbound messages** — fix for non-delivery of contract signing links. In production a `/contracts/:id/send-whatsapp` call logged `url generation failed` immediately after the `→ sock.sendMessage` and immediately before the ack line; the message returned `msgId=3EB0764CF03572ACCE23DF` with `status=1` (PENDING) but never reached the client. Root cause: Baileys' `generateLinkPreviewIfRequired` (see `node_modules/@whiskeysockets/baileys/lib/Utils/messages.js:34-46` and `:266`) tries to fetch any URL detected in `message.text` to generate og:title/og:image/og:description preview metadata. When the fetch fails or times out (Vercel cold start, JS-only signing page with no og: tags, Railway warmup), three things happen: (1) "url generation failed" log spam, (2) multi-second synchronous wait that pushes encryption past a Signal-protocol session-rotation boundary and corrupts the in-flight message, (3) WhatsApp's anti-spam scoring is harsher on link-preview-bearing messages from non-Business accounts. Per the Baileys source, if `message.linkPreview` is anything other than `undefined`, the preview path is skipped entirely. Fix: extracted a tiny `buildOutboundTextMessage(text)` helper in `server/lib/baileys/sessionManager.js` that returns `{ text, linkPreview: null }` and routed the single `sock.sendMessage` call through it. We never wanted previews anyway — recipients see a tappable URL in the text body, and that's all we need for contract-signing, smart-quote, and post-accept notes. Tests: `server/__tests__/baileysLinkPreview.test.js` × 3 (short body, URL-bearing body matching the production message shape, empty string). Full suite 414/414 passing. **Follow-up if delivery still fails after this**: subscribe to `messages.update` to log real delivery status (`status=2/3/4` = SERVER_ACK/DELIVERY_ACK/READ) so we can distinguish "queued and lost" from "delivered but client filtered". Also consider an agency-branded short URL — Vercel/Railway hostnames sometimes get auto-flagged by WhatsApp anti-phishing. |
| v1.14.21 | pending | **Tighten empty-lead guard — drop force-stamped `new_lead` with no real content**. v1.14.19 dropped messages where `extractedData` was null, but not the case where Claude vision returned a *parseable-but-useless* JSON for non-document images (stickers, selfies, scenic photos, screenshots). The WA handler force-stamped `extractedData.classification = 'new_lead'` on the empty object, making it truthy, so the old null-check let it through and we got "Nouveau lead" cards with no name / no document / no dates / no summary. Confirmed in production log: `sender=...@lid \| image=true \| text="" → extraction result: classification=new_lead \| confidence=undefined → ✓ lead inserted`. Fix: new `hasSubstantiveContent(extracted)` helper rejects extracted objects whose only contents are meta fields (`classification`, `confidence`, `confidenceScores`, `lastDocumentType`, `rentalIntent.detected=false`). A lead is substantive iff at least one of: classification ∈ {prolongation, support_issue, alert}, firstName/lastName, any document number (cin/license/passport/generic), start_date/end_date/requested_car, rentalIntent.detected=true, summary_for_agent. Replaces the `!extractedData` guard in both `handleInboundWhatsApp` and the Gmail webhook handler. Tests: `server/__tests__/emptyLeadGuard.test.js` +10 cases (image-with-empty-JSON drops, plus 9 unit tests for `hasSubstantiveContent` covering each kept/dropped path). Full suite 411/411 passing. Staging cleanup SQL also runs against the new shape: `UPDATE pending_demands SET status='ignored' WHERE status='pending' AND extracted_data IS NULL`. The new guard doesn't retroactively touch the rows the prior bug created — clean those up via the same SQL widened: `... OR jsonb_typeof(extracted_data) = 'object' AND extracted_data ?& array['classification'] AND NOT (extracted_data ?| array['firstName','lastName','cinNumber','drivingLicenseNumber','passportNumber','documentNumber','start_date','end_date','requested_car','summary_for_agent']) AND extracted_data->>'classification' = 'new_lead'`. |
| v1.14.20 | pending | **Prolongation lead context + car-availability guard + safe convert routing**. (1) `LeadModal` prolongation block now surfaces the contract context the agent needs to triage: **Numéro de contrat**, **Date de début**, **Date de fin initiale** — sourced from `targetContract` (already loaded for the existing "Contrat … — vehicle — client" line, just unrendered). (2) New `lib/db.js#findVehicleConflicts(vehicleId, startDate, endDate, excludeContractId)` queries other `status='active'` contracts on the same vehicle using the same half-open overlap predicate as `get_available_vehicles` (`pickup_date < endDate AND return_date > startDate`). Extension window is `[contract.endDate, extracted.end_date]`, with `addDays(contract.endDate, requested_extra_days)` fallback when only the extra-days field is set. (3) On conflict, LeadModal renders an amber warning ("⚠ Véhicule indisponible — {vehicle} déjà loué du {start} au {end}") plus a "Proposer un autre véhicule" CTA that reveals `SmartQuotePanel` inline. SmartQuotePanel already uses `get_available_vehicles` so the conflicted car never appears in the alternative-vehicle dropdown — the existing rule is the source of truth. `Prolonger contrat →` stays enabled (soft warn — agent has final say). (4) `pages/Basket.jsx#handleConvert` defense-in-depth: refuses to navigate to `/new-rental` for any lead with `classification='prolongation'` (logs warn + returns). The LeadModal already gates its CTAs by classification, but any future code path that wires `onConvert` to a prolongation lead is now blocked from creating a duplicate brand-new contract. (5) i18n: 6 new `panel.prolongation*` keys × fr/ar/en. Tests: 5 new in `src/test/leadModalProlongation.test.jsx` (contract dates render, findVehicleConflicts called with correct window, warning + CTA appear on conflict, smart quote reveals on click, no warning when no conflict), 6 in `src/test/findVehicleConflicts.test.js` (input guards, overlap predicate, neq filter, error path), 3 in `src/test/basketConvertGuard.test.js` (new_lead navigates, prolongation in extracted_data blocks, prolongation in lead.classification blocks). Full suite 401/401 passing. |
| v1.14.19 | `4d635d0` | **Drop messages with no extractable content** — stops empty corbeille cards ("Numéro masqué (WhatsApp) — Aucun document extrait"). Production log confirmed `[pipeline:wa] ← sender=...@lid | image=false | text=""` jumping straight to `match: none — new lead` → `✓ lead inserted` with `extracted_data: null`. `handleInboundWhatsApp` and `POST /webhook/gmail` had no early-return for the body-empty + image-null path (stickers, videos, reactions, locations, contacts, autoresponders, etc.); they also fell through when image-vision returned null OR when the text classifier returned null on a `preFilter='ok'` branch with no ambiguous fallback. Fix: single safety guard before `findMatchingDemand` in both handlers — `if (!extractedData) { log + return }`. Invariant is now explicit: no `pending_demands` row without `extracted_data`. Tests: `server/__tests__/emptyLeadGuard.test.js` × 4 (@lid + empty, whitespace-only body, null body, group sender). Full suite 387/387 passing. Staging cleanup SQL: `UPDATE pending_demands SET status='ignored' WHERE status='pending' AND extracted_data IS NULL`. |
| v1.12.7 | pending | **Silence Baileys `Timed Out` init log spam**. Symptom: Railway logs showed periodic pino errors from `fetchProps` → `executeInitQueries` → `promiseTimeout` (Baileys `chats.js:631` / `generics.js:131`), `err.type: Error`, `err.message: "Timed Out"`, `isBoom: true`. Triggered whenever WhatsApp's servers don't respond to the post-connect props query within `defaultQueryTimeoutMs` (60s). The rejection is thrown from inside Baileys' `EventBuffer` listener — not awaitable from our side, so it always surfaced as an unhandled error in the logger we hand to `makeWASocket`. Fix: pass `fireInitQueries: false` to `makeWASocket` in `server/lib/baileys/sessionManager.js`. This skips the entire post-connect init batch (`fetchProps` + `fetchBlocklist` + `fetchPrivacySettings`) — those exist for "WA Web parity" and aren't used for messaging. No-op for our send/receive flow. No test added: the change is a single config flag and the only meaningful assertion (that `makeWASocket` was called with the flag) is theater. |
| v1.12.6 | `ebca075` | **Sidebar basket badge now actually appears**. v1.12.5 polled `supabase.from('leads').select(count)` directly from the anon client — RLS on `pending_demands`/`leads` was silently returning `count=null`, so the badge never rendered even when 4 pending leads existed (visible in the Corbeille `Leads` tab counter). Switched `components/Sidebar.jsx` to call `api.getLeads('pending')` + `api.getAlerts()` (filtered to `status='pending'`) through the Railway backend, which uses service_role and filters by `req.user.agency_id`. Identical data source as the Basket page, so the sidebar number always matches what the agent sees inside. Realtime channel dropped (1s poll is enough, and we no longer have a Supabase channel to subscribe to). |
| v1.12.5 | `dab87ba` | **Two UX tweaks**. (1) **LeadModal Ignorer is now instant** — `components/LeadModal.jsx` closes the modal first and fires `onStatusChange(lead.id, 'ignored')` as a background promise (errors logged to console). Previous flow awaited the API round-trip before calling `onClose()`, which (a) left the modal visible during the request and (b) silently kept it open if the API threw. Also removed the unused `ignoring` state and "Suppression…" loading label since the modal is gone before the request resolves. (2) **Sidebar basket badge polls every 1s** — `components/Sidebar.jsx` `setInterval(refresh, 5000)` → `setInterval(refresh, 1000)` so new pending leads/alerts surface in the Corbeille badge within a second even when the Supabase Realtime channel is down. The count query is a `head: true` `count: 'exact'` lookup on `leads` filtered by `agency_id` + `status='pending'` — cheap, but ~3600 reads/hour per active session. |
| v1.12.3 | pending | **Silent WhatsApp send failures for @lid leads + diagnostics**. Symptom: Railway logs showed `[pipeline:offer] → Baileys message sent to 7383233388632` and `✓ lead → offer_sent`, but the client never received the message. Root cause: `server/routes/whatsapp.js:147` did `lead.sender_id.replace(/@.*$/, '').replace(/\D/g, '')` and `formatWhatsAppNumber` in `server/lib/twilioClient.js` re-attached `@s.whatsapp.net` to whatever digits remained. For `@lid` privacy JIDs (where the digits are NOT a phone number), this produced a non-existent address like `7383233388632@s.whatsapp.net`. Baileys queued it without error, WhatsApp dropped it. Fix: (1) `formatWhatsAppNumber` now passes through any input that already contains `@` — strips multi-device `:NN` suffix only, preserves both `@s.whatsapp.net` AND `@lid`. (2) `whatsapp.js` no longer pre-strips `sender_id` — passes the raw JID through. (3) `sessionManager.sendMessage` now calls `sock.onWhatsApp(jid)` as a pre-flight check (logs a warning + uses the resolved canonical JID when available), and logs the requested/resolved JID + ack details after every send. (4) Warning log when `sender_id` ends with `@lid` — Baileys cannot always deliver to those without LID→phone resolution. Tests: 12 new (`server/__tests__/twilioClient.test.js`). Full suite 217/217 passing. |
| v1.12.2 | pending | **Two UX fixes**. (1) **LeadModal Ignorer didn't close the modal** — `components/LeadModal.jsx` now calls `onClose()` after the status update so the agent moves on to the next lead immediately. (2) **Sidebar basket badge needed a polling fallback** — the Realtime subscription works when the websocket is healthy, but silently falls behind if it drops. Added a 5-second `setInterval` refresh alongside the existing postgres_changes channel so the count converges even when Realtime is unavailable. The count already includes leads + alerts + escalated alerts (every row with `status='pending'`); the polling guarantees the agent sees new arrivals promptly without depending on the WS. |
| v1.12.1 | pending | **Three bug fixes**. (1) **Audio voice notes were creating empty leads** — Baileys handler in `server/lib/baileys/sessionManager.js` only extracted text from `conversation`/`extendedTextMessage`/`imageMessage.caption`, completely ignoring `audioMessage`/`pttMessage`. New `server/lib/transcribe.js` wraps OpenAI Whisper (`openai` v4.104.0 was already installed); sessionManager now downloads the audio buffer and transcribes it into `bodyText` before calling `handleInboundWhatsApp`, so voice notes flow through the same triage + classification pipeline as text. Requires `OPENAI_API_KEY` env var (graceful fallback to null when unset). (2) **Escalader didn't unlock the smart quote** — escalated alerts land at `status='pending'` but `LeadModal`'s "Préparer Devis" button was gated on `extracted.classification === 'new_lead'`, which alerts never have. Dropped the classification gate; any pending lead can now be moved to `waiting` and a quote sent. (3) **JID display garbage** (`84139063677034@lid` shown raw next to a formatted version) — new `utils/phoneFormat.js` strips `@s.whatsapp.net`/`@lid`/`@c.us` + device suffix and formats digits as `+CC XXX XXX XXX`. Applied across `LeadModal.jsx`, `pages/Basket.jsx` LeadCard, and `AlertCard.jsx`. Tests: 16 new (`src/test/phoneFormat.test.js` ×10, `server/__tests__/transcribe.test.js` ×6). Full suite 205/205 passing. |
| v1.12.0 | pending | **Premium tier removed**: all agencies have full feature access. (1) Backend: deleted `server/middleware/premium.js`; removed `requirePremium` from `server/routes/leads.js` and `server/routes/gmail.js`; dropped `.eq('plan','premium')` filter on the gmail poll-all loop. (2) Frontend: removed `isPremium`/`useIsPremium` from `lib/UserContext.js`, the basket redirect guard + provider field in `App.jsx`, the `premium: true` flag and PRO badge in `components/Sidebar.jsx`, the upgrade wall in `pages/Basket.jsx`, the PREMIUM badge in `pages/settings/IntegrationsTab.jsx`. (3) Tests: removed `requirePremium` mock blocks from `offerResponse.test.js` + `smartQuote.test.js` (all 189 tests pass). (4) DB: migration `20260521b_remove_premium_gates.sql` normalizes existing rows to `plan='premium'` + `seat_limit=NULL`. `agencies.plan` column retained for future tiered pricing. |
| v1.10.7 | pending | **Phase 5b / Law 09-08 frontend cutover (flag STILL OFF)**: client CRUD moves off `lib/db.js` direct Supabase reads onto the new `/clients` API. (1) `lib/api.js` renames `getClientsApi`/etc → `getClients`/`getClient`/`saveClient`/`patchClient`/`deleteClient`. (2) Call sites switched: `pages/Clients.jsx` (getClients×2 + saveClient×3), `pages/Dashboard.jsx`, `pages/Contracts.jsx`, `pages/Invoices.jsx`, `pages/ContractSuccess.jsx` (getClient), `pages/rental/ContractStep.jsx` (saveClient). (3) `lib/db.js` removes `getClients`, `saveClient`, `deleteClient`, `getClient`, and the `clientToDb`/`clientFromDb` mappers. Backend still reads/writes plaintext columns — v1.10.8 flips the flag. **Heads-up:** every client read now round-trips through Railway instead of Supabase direct — minor latency tradeoff for the encryption guarantee. |
| v1.10.6 | pending | **Phase 5a / Law 09-08 encryption-at-rest groundwork (flag OFF)**: (1) `server/lib/encryption.js` extracts AES-256-GCM helpers from `server/routes/leads.js`; lazy key resolution + `isEncryptedBlob()`. (2) `server/lib/encryption.test.js` — 10 cases (round-trip, fresh IV, unicode, null-passthrough, plaintext-passthrough, tag-tamper, dev-fallback path). (3) `server/routes/leads.js` refactored to import from the shared module (re-exports for callers). (4) Migration `20260515b_clients_encrypt.sql` adds `id_number_enc / driving_license_num_enc / date_of_birth_enc` text columns (nullable). (5) `server/routes/clients.js` — new backend CRUD (GET / GET:id / POST / PATCH / DELETE) with `ENCRYPT_PII` feature flag. When false (default) reads/writes the legacy plaintext columns — zero behavior change. (6) `server/scripts/migrateClientsEncryption.js` + test (7 cases): idempotent batched backfill of `_enc` columns from plaintext; refuses to run without `ENCRYPTION_KEY`. (7) `server/lib/anonymize.js` updated to also null the new `_enc` columns so Phase 3 erasure stays complete regardless of flag state. (8) `lib/api.js` adds `getClientsApi / getClientApi / saveClientApi / patchClientApi / deleteClientApi` (frontend NOT yet switched — that's v1.10.7). (9) `enforce:retention` companion: new `migrate:clients-encrypt` npm script. **Rollout:** ship 5a, run data migration on the side, then v1.10.7 switches frontend to api.\*, then v1.10.8 flips `ENCRYPT_PII=true` in Railway. |
| v1.10.5 | `0c05399` | **Phase 4 / Law 09-08 retention automation**: (1) migration `20260515_agencies_retention.sql` adds `agencies.retention_years int NOT NULL DEFAULT 10 CHECK (5-30)`. (2) New `server/lib/anonymize.js` extracts the anonymize-client logic shared between `admin.js` and the new retention cron — same audit_log writes, distinct `action` field (`client.anonymize` vs `client.anonymize.retention`). (3) `server/scripts/enforceRetention.js` + `enforceRetention.test.js` (8 cases): per-agency loop, picks oldest admin as audit actor, anonymizes clients whose contracts are all closed and whose latest `closed_at` is past the retention window. (4) `server/routes/agency.js` PATCH now accepts `retention_years` with 5-30 validation. (5) `pages/settings/PrivacyTab.jsx` adds a retention-period input (admin-only, reuses `api.updateAgency`). (6) `server/index.js` wires monthly cron at `30 4 1 * *`. (7) `package.json` adds `enforce:retention` npm script. |
| v1.10.4 | `8ecc7f0` | Perf + bugfix: (1) `lib/db.js` `getAgencyId()` now caches per-user in module scope and uses `auth.getSession()` (localStorage, no network) instead of `auth.getUser()` — eliminates ~600-1000 ms latency from every db.js call, fixing the slow vehicle-fetch on NewRental; cache invalidated on `SIGNED_OUT`/`USER_UPDATED`. (2) `pages/SignContract.jsx` — moved `drawing`/`hasStrokes` flags from `useState` to `useRef` during the canvas drag (state-flip on `stopDraw` only), preventing the mid-draw re-render that was clearing the canvas bitmap on stroke release. |
| v1.10.3 | `14512d4` | Fix: Simulate-scan buttons stripped from prod build (DEV gate); also `t('privacy.scanNotice')` → `t('common:privacy.scanNotice')` namespace fix |
| v1.10.2 | `6ff74be` | feat(scan): dev-only simulate buttons for CIN+licence scans (regression: DEV gate compile-time stripped from staging build — see v1.10.3) |
| v1.9.0 | pending | Booking Hub: omnichannel reservations table — new `reservations` Supabase table with enums (booking_source, reservation_status), RLS, 6 indexes; Express GET/POST/PATCH `/reservations` with server-side filters/sort/pagination; React page using TanStack Table v8 + Query v5; FilterBar (channel, status, search, date range, price range); SourceChannelBadge (Mail/MessageCircle/Globe/User), StatusBadge; ReservationDetailsPanel (side sheet w/ source_metadata JSON viewer); date-fns + date-fns-tz timezone utils (UTC ↔ user-local); FR/AR/EN i18n; new Sidebar nav "Réservations"; NewRental wizard now creates a reservation row on completion with channel auto-detection (WHATSAPP/EMAIL from prefilledLead.source, else IN_PERSON) |
| v1.8.1 | 84d63c0 | Bugfix: Staff role access — hide Settings nav from staff (ADMIN_ONLY_PAGES) (note: premium gate referenced in this entry was removed in v1.12.0) |
| v1.8.0 | pending | Feature bundle (6): (1) Arabic RTL — `<html dir>` now synced on i18n init + sidebar nav flip CSS; (2) async team invitations — TeamTab fires `inviteMember` without awaiting, toast appears immediately; (3) staff signup flow — Onboarding now blocks invited users on a password-setup form before silent agency join; (4) role-based Settings — staff role sees only the Privacy tab, admin/manager unchanged; (5) Sidebar Basket unread badge — live count of `status=pending` leads via Realtime subscription; (6) ID document mismatch modal — ScanStep now compares CIN/passport names vs licence names and surfaces a Cancel/Continue modal on divergence |
| v1.7.1 | pending | Fix(e-sig): unsigned PDF upload was hitting Storage RLS. Moved upload to backend (service_role) — frontend now sends pdf_base64 via /contracts/:id/send-whatsapp; no storage policies needed |
| v1.7.0 | `67e7e89` | Feat: custom e-signature flow — manager generates unsigned PDF (jsPDF) + uploads to Supabase Storage, backend mints UUID signing token + dispatches WhatsApp link via Twilio, public /?sign=<token> page captures signature on HTML5 canvas, backend stamps PDF with pdf-lib at bottom-right of last page + uploads stamped version, manager dashboard subscribes via supabase.channel postgres_changes for instant "Terminer" enable. Migration adds signature_status/signing_token/signed_pdf_url to contracts. New server dep: pdf-lib@^1.17.1 |
| v1.6.1 | `e0158b0` | Fix(NewRental drafts): RentalStep/PhotoStep/ContractStep now pass their state to onSaveAndQuit (was passing the React click event); empty-draft check now inspects field values instead of object truthiness so empty stub objects no longer create phantom drafts |
| v1.6.0 | `eb4e85b` | Bug bundle: (1) team invite role validation + agency join fix (data loss bug) + email timing logs, (2) language selector moved to merged Settings → Configuration générale (3 tabs collapsed), (3) DocumentExpiryAlert modal on ScanStep for expired CIN/license, (4) NewRental draft persistence + draft picker grid with "+ New Rental" card |
| v1.5.0 | `ac80965` | Phase 3 / Law 09-08: right to erasure endpoint, audit_log table, Settings → Confidentialité tab (admin), Clients.jsx anonymization guards, FR+AR i18n |
| v1.4.1 | `c864768` | Phase 2 / Law 09-08: pending demands cleanup script, anonymized_at migration, cleanup:pending npm script |
| v1.4.0 | `a3a3dd5` | Phase 1 / Law 09-08 compliance: CNDP privacy notice on ScanStep, full PrivacyPolicy.jsx page (FR + AR), Sidebar footer link |
| v1.3.1 | `6a86168` | Design system overhaul: Mastercard-inspired (Canvas Cream, Ink Black, pill radii, Sofia Sans, Signal Orange consent-only) — index.css full token rewrite + Sidebar + 7 pages |
| v1.3.1 | `5b9d658` | Fix classifyTextMessage max_tokens 256→512 (JSON truncation bug causing SmartQuotePanel to never appear) |
| v1.2.3 | pending | Smart Quote: available-vehicles only, start/end date fields, notes field, channel-aware send button (Email vs WhatsApp), POST /email/send-offer via Resend |
| v1.2.2 | `22a561a` | Fix Escalader: sets classification='lead' + status='pending' so escalated alerts appear in Leads tab |
| v1.2.1 | `ea88635` | Fix triage pre-filter: add semaine/semaines/journée/week/weeks/woche to MEDIUM, renseigner/besoin/need to LOW |
| v1.2.0 | `3533bbf` | Alerts Dashboard: orange widget on Dashboard, Leads/Alertes tabs in Basket, triage pipeline, DB migration `002_alerts_classification.sql` |
